/**
 * The product's name, in one place.
 *
 * It has been spelled three ways at once — "Adaptive LP" in the interface,
 * "pagetive" in the repository, "adaptive-lp" in the page title a visitor
 * actually sees — and every screen that hardcodes one of them is a place the
 * next rename misses. The tagline lives here for the same reason: it is used
 * by the sidebar, the sign-in panel and the social card, and those three
 * disagreeing is how a product looks unfinished.
 */
export const PRODUCT_NAME = "Pagetive";

export const PRODUCT_TAGLINE = "one page, many versions";

export const PRODUCT_DESCRIPTION =
  "Describe a landing page in chat. It gets built as typed blocks, matched to the ad that sent each visitor, and it keeps testing itself.";
